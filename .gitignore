# --- Python & Virtual Env ---
.venv/
venv/
# Note: 'Scripts/' (capital S) is the venv folder on Windows — use exact path to avoid
# accidentally ignoring project-level 'scripts/' folder (lowercase)
.venv/Scripts/
venv/Scripts/
Lib/
Include/
__pycache__/
*.py[cod]

# --- Model Weights (TUYỆT ĐỐI KHÔNG ĐẨY LÊN) ---
# Chúng ta giấu những file 6GB này đi vì GitHub không chứa nổi
_tmp_out/
checkpoint/
lora_model/
gguf/
*.safetensors
*.bin
*.pt

# --- Dataset & Logs ---
finetune/datasets/*.jsonl
*.log
storage/
*.csv

# --- Runtime / Temp ---
memory/brain_gate.json
memory/brain_gate.lock
memory/brain_gate.error.json
tests/live_test_log.txt
copilot_body_test2.json
*.error.json

# --- Frontend Build ---
frontend/.next/
frontend/node_modules/